#==========================#
#     Medications Card     #
#==========================#

type: custom:stack-in-card
title: Medications
keep:
  margin: true
cards:
      
  # Eliquis
  - type: 'custom:decluttering-card'
    template: next_event
    variables:
      - entity: input_datetime.reminder_medication_eliquis
      - name: Eliquis
      - icon: pill
      - interval: 90
      - period: 'days'
      - service: script.set_datetime_offset_days
      
  # Gabapentin
  - type: 'custom:decluttering-card'
    template: next_event
    variables:
      - entity: input_datetime.reminder_medication_gabapentin
      - name: Gabapentin
      - icon: pill
      - interval: 90
      - period: 'days'
      - service: script.set_datetime_offset_days
      
  # Metoprolol
  - type: 'custom:decluttering-card'
    template: next_event
    variables:
      - entity: input_datetime.reminder_medication_metoprolol
      - name: Metoprolol
      - icon: pill
      - interval: 90
      - period: 'days'
      - service: script.set_datetime_offset_days
      
  # Rosuvastatin
  - type: 'custom:decluttering-card'
    template: next_event
    variables:
      - entity: input_datetime.reminder_medication_rosuvastatin
      - name: Rosuvastatin
      - icon: pill
      - interval: 90
      - period: 'days'
      - service: script.set_datetime_offset_days
      
  # Blood Builder Minis
  - type: 'custom:decluttering-card'
    template: next_event
    variables:
      - entity: input_datetime.reminder_medication_blood_builder_minis
      - name: Blood Builder Minis
      - icon: pill
      - interval: 36
      - period: 'days'
      - service: script.set_datetime_offset_days
      
  # Methyl B12
  - type: 'custom:decluttering-card'
    template: next_event
    variables:
      - entity: input_datetime.reminder_medication_methyl_b12
      - name: Methyl B12
      - icon: pill
      - interval: 90
      - period: 'days'
      - service: script.set_datetime_offset_days