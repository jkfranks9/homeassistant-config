#==================#
#     InfluxDB     #
#==================#

host: !secret influx_ip_addr
port: 8086
database: home-assistant
username: !secret influx_username
password: !secret influx_password
max_retries: 3
default_measurement: state

# NOTES: 
# - Identical to recorder configuration.

#==== Exclusions

exclude:
  
  domains:
    - automation
    - binary_sensor
    - button
    - calendar
    - camera
    - counter
    - fan
    - group
    - input_boolean
    - input_datetime
    - input_number
    - input_select   
    - input_text
    - light
    - media_player
    - number
    - proximity
    - remote
    - scene
    - script
    - select
    - sensor  
    - sun
    - switch
    - timer
    - update
    - vacuum
    - weather
    - zone
  
  entities:
    - device_tracker.international_space_station_iss
    - input_number.variables_workout_time

#==== Inclusions

include:

  domains:
    
    - climate
    - cover
    - device_tracker
    - person

  entities:
    
    - binary_sensor.dryer_vibration_sensor_vibration
    - binary_sensor.office_human_sensor_presence
    - input_boolean.office_occupied
    - input_number.washer_fill_seconds
    - input_select.washer_load_size
    - input_select.washer_state
    - sensor.ahi_average
    - sensor.cpap_ahi_events_per_hour
    - sensor.cpap_average
    - sensor.cpap_usage_hours
    - sensor.cpap_usage_minutes
    - sensor.downstairs_humidity
    - sensor.dryer_humidity
    - sensor.nighttime
    - sensor.porch_humidity
    - sensor.sonoff_washer_power
    - sensor.total_daily_energy
    - sensor.total_power
    - sensor.washer_power_state
  
  entity_globs:
    
    - binary_sensor.*_occupancy
    - binary_sensor.family_room_av_*
    - binary_sensor.theater_av_*
    - input_number.*_calories
    - input_number.*_time
    - sensor.*_ambient_light
    - sensor.*_temperature
    - sensor.total_*_calories
    - sensor.total_*_time
