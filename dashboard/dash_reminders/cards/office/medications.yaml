#==========================#
#     Medications Card     #
#==========================#

type: custom:stack-in-card
title: Medications
cards:
      
  # Eliquis
  - type: 'custom:decluttering-card'
    template: next_event
    variables:
      - entity: input_datetime.reminder_medication_eliquis
      - name: Eliquis
      - icon: pill
      - interval: 90
      - period: 'days'
      - service: script.set_datetime_offset_days
      
  # Gabapentin
  - type: 'custom:decluttering-card'
    template: next_event
    variables:
      - entity: input_datetime.reminder_medication_gabapentin
      - name: Gabapentin
      - icon: pill
      - interval: 90
      - period: 'days'
      - service: script.set_datetime_offset_days
      
  # Metoprolol
  - type: 'custom:decluttering-card'
    template: next_event
    variables:
      - entity: input_datetime.reminder_medication_metoprolol
      - name: Metoprolol
      - icon: pill
      - interval: 90
      - period: 'days'
      - service: script.set_datetime_offset_days
      
  # Rosuvastatin
  - type: 'custom:decluttering-card'
    template: next_event
    variables:
      - entity: input_datetime.reminder_medication_rosuvastatin
      - name: Rosuvastatin
      - icon: pill
      - interval: 90
      - period: 'days'
      - service: script.set_datetime_offset_days
      
  # Blood Builder Minis
  - type: 'custom:decluttering-card'
    template: next_event
    variables:
      - entity: input_datetime.reminder_medication_bloodbuilder
      - name: Blood Builder Minis
      - icon: pill
      - interval: 72
      - period: 'days'
      - service: script.set_datetime_offset_days
      
  # Methyl B12
  - type: 'custom:decluttering-card'
    template: next_event
    variables:
      - entity: input_datetime.reminder_medication_methylb12
      - name: Methyl B12
      - icon: pill
      - interval: 33
      - period: 'weeks'
      - service: script.set_datetime_offset_weeks
